import 'babel-register';
import test from "ava";
// import { reduce } from "../src/index";

test('foo', t => {
  t.pass();
});

/*
  possible ava assertions: pass/fail([msg]), truthy/true/falsy/false(v, [msg]),
  is/not/deepEqual/notDeepEqual(v, expected, [msg]), throws(f()|promise, [err, [msg]])
*/
